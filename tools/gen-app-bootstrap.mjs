/**
 * Раньше собирал приложение из `legacy-monolith.js`.
 * Монолит удалён: правьте модули в `frontend/scripts/app/` (`app.js`, `workspace.js`, `export.js`, …).
 */
console.error(
  "gen-app-bootstrap: legacy-monolith.js удалён. Редактируйте модульную структуру в frontend/scripts/app/."
);
process.exit(1);
